{
  "description": "Query a data product (no email)",
  "required_params": ["name", "agent_id", "data_product_id", "query"],
  "definition": {
    "nodes": [
      {
        "id": "start",
        "type": "type_start",
        "data": {
          "inputs": {},
          "config": {"description": "Entry point"}
        }
      },
      {
        "id": "query_data",
        "type": "type_agent",
        "data": {
          "inputs": {
            "message": {
              "type": "static",
              "value": "{{QUERY}}"
            },
            "data_product_id": {
              "type": "static",
              "value": "{{DATA_PRODUCT_ID}}"
            }
          },
          "config": {
            "agent_id": "{{AGENT_ID}}",
            "description": "Query data product"
          }
        }
      }
    ]
  }
}
